version: 2
jobs:
  build:
    docker:
      - image: python:3.6
    steps:
      - checkout
      - run: pip install -q -r requirements.txt
      - run: pytest -v --cov . --pep8 --junitxml=test_reports/junit.xml
      - store_test_results:
          path: test_reports
